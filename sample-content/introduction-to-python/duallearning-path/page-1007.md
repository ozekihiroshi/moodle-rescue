## Introduction

**A number alone does not communicate its work meaning**

The previous lesson calculated the value 80.6. Displayed alone, it could be a completion rate, a cost, or something else. Programs must combine calculated values with names, descriptions, and units for human readers. Python represents this text with type `str`.

## Learning outcomes

At the end of this lesson, you can:

- Create strings and read characters or slices by position.
- Explain that string methods return new values rather than modifying the original string.
- Accept input as text and convert it before numeric calculation.
- Produce labelled, formatted output with f-strings.
- Split a simple delimited record while recognising that full CSV needs a CSV library.

**Learning route:** Required: 1.4.1–1.4.4 \| Supporting: 1.4.5 \| Integration: 1.4.6

## 1.4.1 Create, inspect, and transform strings

### Quotes mark the extent of a string

Single and double quotes both create strings. Choose outer quotes that keep quoted content readable. A newline is written `\n`, a tab `\t`, and a literal backslash `\`.

```
course = "Introduction to Python"
message = 'The teacher said, "Run and check."'
print(course)
print(message)
print("Line 1\nLine 2")
```

A missing closing quote usually raises `SyntaxError` because Python cannot identify where the string ends. Quotes and escapes distinguish string content from Python syntax.

### A string is a sequence whose positions begin at zero

Each character has a position. The first is position zero and the next is one. Negative positions count from the end, so `-1` selects the last character.

```
centre = "North Centre"
print(centre[0])
print(centre[-1])
print(centre[0:5])
print(len(centre))
```

A slice such as `centre[0:5]` includes the start and excludes the end, returning `North`. `len()` returns the character count. Reading one character outside the available range raises `IndexError`, while a slice returns the available portion.

### Strings are immutable, so processing produces a new value

A character cannot be replaced with `centre[0] = "n"`. Strings are immutable. Instead, create a processed string and assign it when it should become current.

```
raw_name = "  North Centre  "
clean_name = raw_name.strip()
print(clean_name)
print(clean_name.lower())
print(clean_name.upper())
print(clean_name.replace("Centre", "Learning Centre"))
print("Original:", raw_name)
```

`strip()` removes surrounding whitespace; `lower()` and `upper()` change case in the returned value; `replace()` returns text with the requested replacement. The original `raw_name` remains unchanged, a useful distinction when cleaning data without losing its source.

## 1.4.2 Combine text and values with compatible types

### Match types before concatenation

`+` joins two strings. Directly adding a string and integer raises `TypeError`. Convert a number with `str()` when concatenation is truly required. `*` repeats a string and can create a separator.

```
completed = 29
print("Completed: " + str(completed))
print("-" * 20)
```

## 1.4.3 Accept input and convert it for calculation

### input() always returns a string

`input()` displays a prompt and waits until the user enters a value. Even if the user types 36, the returned value is text `"36"`. Convert with `int()` or `float()` before numeric calculation.

```
registered_text = input("Registered learners: ")
registered = int(registered_text)
print(registered + 4)
```

A Notebook cell containing `input()` waits for entry; a persistent `[*]` can mean the kernel is waiting. The supplied Notebook comments the interactive lines for automatic verification and provides equivalent assigned text. Empty or non-numeric input can raise `ValueError` during conversion. Conditions and exceptions later extend this into safe input handling.

**Quick check:** What type does input() return even when the learner types digits?

## 1.4.4 Produce meaningful formatted output

### f-strings combine meaning and values with a display format

Prefix a string with `f` and Python evaluates names or expressions inside braces. `.1f` displays one decimal place, `.2f` two, and `,` adds a thousands separator.

```
centre = "North Centre"
registered = 36
completed = 29
rate = completed / registered * 100
cost = 12345.5
print(f"{centre}: {completed}/{registered} learners, completion {rate:.1f}%")
print(f"Materials cost: {cost:,.2f}")
```

Formatting changes the displayed representation, not the underlying values in `rate` or `cost`. Keep calculation values distinct from presentation.

**Quick check:** What information must accompany a number so that another person can understand the output?

## 1.4.5 Read simple delimited text

### Split delimited text into fields

`split()` separates a string at a delimiter and returns a list of strings. Splitting `"North,36,29"` at commas makes the name and two counts available. Lists are developed systematically in their own chapter.

```
record = "North,36,29"
parts = record.split(",")
centre = parts[0]
registered = int(parts[1])
completed = int(parts[2])
print(f"{centre}: {completed}/{registered} learners")
```

## Integrated practice: a lighter rehearsal for the chapter project

**Connection to the chapter project:**

Accept a parcel record and turn input text into a calculated, formatted line.

### What to build

Read destination, parcel count, and price per parcel. Display a line such as `Kumasi: 6 parcels, total 4500`.

### Completion criteria

- Use three `input()` calls.

- Strip the destination and convert both numeric inputs.

- Keep input, calculation, and presentation separate.

### Step-by-step guide

1.  `input()` returns text.

2.  Use `int()` for whole numbers.

3.  Use one f-string for the result.

**Build and run your own version before opening the model answer.**

> [!ANSWER]
> **Model answer and explanation**
>
> ```
> destination = input("Destination: ").strip()
> parcels = int(input("Parcels: "))
> price = int(input("Price per parcel: "))
> total = parcels * price
> print(f"{destination}: {parcels} parcels, total {total}")
> ```
>
> **Representative check**
>
> ```
> Kumasi: 6 parcels, total 4500
> ```
>
> Conversion happens at the program boundary; calculation then uses numbers and the f-string controls presentation.

### Change and recheck

Run the unchanged program with another destination and numbers. Do not fix sample answers in the code.

## Summary

After completing this lesson and its integrated practice, you can:

- Treated text as an ordered, immutable sequence.
- Matched types deliberately when combining text and numbers.
- Connected keyboard input, conversion, calculation, and formatted output.

## Next

Lesson 1.5 uses comparisons and Boolean logic to choose what a program should do with the values it has calculated.

**Estimated learning time:** about 2.5 hours
