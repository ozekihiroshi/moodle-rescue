## Introduction

**Exchange records safely beyond one program run**

Values used so far disappeared when a program ended. Operational work must read earlier records and save results for a later run. Here a small book catalogue is read from CSV, converted to Python records, and written to a separate CSV after a change.

## Learning outcomes

At the end of this lesson, you can:

- Resolve file paths from a defined base rather than an assumed current directory.
- Open and close text files safely with an explicit mode and UTF-8 encoding.
- Read CSV with DictReader and convert field values according to a declared schema.
- Validate the header and every row before using the records.
- Write a separate CSV with DictWriter and verify the saved product by re-reading it.

**Learning route:** Required: 2.3.1–2.3.5 \| Integration: 2.3.6

## 2.3.1 Resolve the intended file path

### Establish the file location before reading

A relative path starts from the current working directory. Display `Path.cwd()` in a Notebook and resolve candidates before changing a path by guesswork. In an independent script, base bundled files on `Path(__file__).resolve().parent` so behaviour does not depend on the launch directory.

```
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
INPUT_PATH = BASE_DIR / "data" / "books.csv"
OUTPUT_PATH = BASE_DIR / "output" / "books_updated.csv"
```

For `FileNotFoundError`, print the resolved absolute candidate and `exists()`. A Notebook normally has no `__file__`, so the supplied Notebook searches upward from its current directory.

## 2.3.2 Open text files with an explicit mode and encoding

### Use with, an intentional mode, and an explicit encoding

A `with` block closes its file on success or failure. Mode `r` reads, `w` creates or replaces, and `a` appends. State `encoding="utf-8"` and, for CSV, `newline=""`. Opening source data with `w` destroys it, so separate input and output paths.

## 2.3.3 Read CSV records without losing their structure

### Do not parse CSV with split

A CSV field containing a comma can be quoted. The title `Data, Decisions, and Evidence` is one field. The standard `csv` module implements quoting, separators, and record endings.

```
import csv

with INPUT_PATH.open("r", encoding="utf-8", newline="") as file:
    reader = csv.DictReader(file)
    print(reader.fieldnames)
    for row in reader:
        print(row)
```

### DictReader values initially remain strings

The header becomes dictionary keys, but CSV text `false` does not become Python `False`. In fact `bool("false")` is `True` because the string is non-empty. Convert by an explicit accepted vocabulary.

```
def parse_read(value):
    text = value.strip().lower()
    if text == "true":
        return True
    if text == "false":
        return False
    raise ValueError(f"read must be true or false: {value!r}")
```

**Quick check:** Why is split(',') not a correct general CSV parser?

## 2.3.4 Validate headers, rows, and converted values

### Validate the header and each row before use

Reject a missing `id`, `title`, or `read` column, blank required value, duplicate ID, or invalid Boolean at the input boundary. Silent substitution lets later code run on false data. Include a line number in the error so the source can be repaired.

## 2.3.5 Save to a separate file and verify by re-reading

### Write a separate CSV and load it again

Give `csv.DictWriter` a stable field order, call `writeheader()`, and convert Booleans back to lower-case text. Create the output folder when needed and leave the supplied input unchanged.

```
OUTPUT_PATH.parent.mkdir(parents=True, exist_ok=True)
with OUTPUT_PATH.open("w", encoding="utf-8", newline="") as file:
    writer = csv.DictWriter(file, fieldnames=["id", "title", "read"])
    writer.writeheader()
    for book in books:
        writer.writerow({"id": book["id"], "title": book["title"], "read": "true" if book["read"] else "false"})
```

A save without an exception does not prove correct columns or conversions. Reload with the same loader and compare count, ID order, titles, and Booleans. Also compare the source bytes before and after.

**Quick check:** Why does re-reading the output verify more than a successful write call?

## Integrated practice: a lighter rehearsal for the chapter project

**Connection to the chapter project:**

Rehearse the project’s read-transform-save cycle with room bookings rather than books.

### What to build

Create a tiny `bookings.csv`, read it with `csv.DictReader`, convert `people` to `int`, add 2 to booking R02, and save a new `bookings_updated.csv` with `csv.DictWriter`.

### Completion criteria

- Keep the input file unchanged.

- Use the exact header `id,room,people`.

- Reopen the output and assert that R02 contains 14.

### Step-by-step guide

1.  Create the small input in the rehearsal code.

2.  Convert CSV text before arithmetic.

3.  Pass `newline=''` and write the header once.

**Build and run your own version before opening the model answer.**

> [!ANSWER]
> **Model answer and explanation**
>
> ```
> import csv
> from pathlib import Path
>
> source = Path("bookings.csv")
> output = Path("bookings_updated.csv")
> source.write_text("id,room,people\nR01,Hall A,8\nR02,Room B,12\n", encoding="utf-8")
> with source.open(encoding="utf-8", newline="") as file:
>     rows = list(csv.DictReader(file))
> for row in rows:
>     row["people"] = int(row["people"])
>     if row["id"] == "R02":
>         row["people"] += 2
> with output.open("w", encoding="utf-8", newline="") as file:
>     writer = csv.DictWriter(file, fieldnames=["id", "room", "people"])
>     writer.writeheader()
>     writer.writerows(rows)
> with output.open(encoding="utf-8", newline="") as file:
>     saved = list(csv.DictReader(file))
> assert saved[1]["people"] == "14"
> print("ROUND TRIP OK")
> ```
>
> **Representative check**
>
> ```
> ROUND TRIP OK
> ```
>
> Reading, typed transformation, separate output, and reopening form one reliable file-processing cycle.

### Change and recheck

Add a room name containing a comma. Confirm that the `csv` module preserves it without manual splitting.

## Summary

After completing this lesson and its integrated practice, you can:

- Separated path resolution, opening, CSV parsing, conversion, and validation.
- Preserved quoted CSV fields that contain commas.
- Protected the source file and validated the output after a complete round trip.

## Next

Project 2.4 combines record structures, tested functions, validation, and CSV input/output in a library-record update program.

**Estimated learning time:** about 3 hours
