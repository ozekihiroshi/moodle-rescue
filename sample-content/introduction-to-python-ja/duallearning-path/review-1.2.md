## 復習：代入の前後を追う

次の三行を上から実行した後、`total`と`price`はいくつですか。紙やMarkdownセルに予想してから、Labで確かめます。

```python
price = 120
total = price * 3
price = 150
```

> [!ANSWER]
> `total`は`360`、`price`は`150`です。`total`には計算した時点の値が代入されています。最後に`price`を変えても、自動で再計算はされません。

説明が難しければ、本文の代入と状態を読み直し、各行の直後の値を書き出します。
