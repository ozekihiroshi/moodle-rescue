## 復習：戻り値と例外を使い分ける

「対象が見つからなければ`None`を返す検索」と「対象がなければ`KeyError`を送出する変更処理」は、同じ扱いでよいでしょうか。2.2の関数契約とテストの例で確認してください。

> [!ANSWER]
> 解答と確認
>
> 契約が異なるので、呼び出す側も区別します。検索では`None`を確認して次の処理を選びます。変更処理では存在する対象が必要であり、対象がなければ指定の例外になります。テストでは正常な戻り値だけでなく、対象がない場合と変更前後の状態も確認します。
