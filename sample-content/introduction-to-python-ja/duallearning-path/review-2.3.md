## 復習：保存できたことを確かめる

出力CSVが存在するだけで、保存は成功したと言えるでしょうか。書名にカンマを含むレコードを例に、2.3.3と2.3.5を読み直してください。

> [!ANSWER]
> 解答と確認
>
> ファイルの存在だけでは、列や値が正しく保存されたとは限りません。別ファイルへ`DictWriter`で保存した後、`DictReader`で再読込し、列名・件数・各値を照合します。カンマを含む書名が一つの値として戻り、入力原本が変わっていないことも確認します。
