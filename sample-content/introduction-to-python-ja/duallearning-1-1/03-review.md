本文のコードをそのまま写せたかではなく、**違う数字でも同じ考え方で確かめられるか**を確認します。Labを開き、以下を上から試してください。

## 自分で確かめる

1. `print(5 + 2 * 3)`と`print((5 + 2) * 3)`は、それぞれ何を表示しますか。予想してから実行します。
2. 1箱に8冊、箱が6箱あるとき、見出し、1箱の冊数、箱数、合計を四行で表示します。合計の`48`は直接書きません。
3. 箱が7箱に変わったら、表示をどこで変更するか説明します。

> [!ANSWER]
> **確認結果と一つの書き方**
>
> 一つ目は`11`と`21`です。二つ目には、例えば次のように書けます。
>
> ```python
> print("BOOK PACKING")
> print("BOOKS PER BOX:", 8)
> print("BOXES:", 6)
> print("TOTAL BOOKS:", 8 * 6)
> ```
>
> 三つ目は、箱数を表示する`6`と、合計を計算する式の`6`を変更します。表示形式やラベルは、四行で意味が明確なら別の書き方でも構いません。

## 止まったとき・あとで復習するとき

Labが実行できないときは「1.1を始める前に」の最初の一行を再実行します。計算は合うのに表示されないときは、式を`print()`に渡しているか確認します。変更後に合計が合わないときは、同じ意味の数が何か所に書かれているか探します。

今日はここまででも構いません。Notebookを`Ctrl + S`で保存し、再開するときはこのコースからLabを開きます。後日、箱数を別の数字に変えてもう一度実行できれば、復習になります。

三つの確認ができたらこの教材を「完了」にしてください。これは自己確認であり、採点やプログラミング技能の認定ではありません。次の1.2では値に名前を付け、同じ数字を何か所も修正する問題を改善します。
